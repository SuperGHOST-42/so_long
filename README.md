# so_long
A small 2D game written in C where you explore the map, collect all items, and only then gain access to the exit. The user provides the map (a matrix) that the game uses to render the world.
